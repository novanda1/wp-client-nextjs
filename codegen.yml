overwrite: true
schema: 'http://localhost:8080/wordpress/graphql'
documents: 'lib/graphql/**/*.graphql'
generates:
    lib/generated/graphql.ts:
        plugins:
            - 'typescript'
            - 'typescript-operations'
            - 'typescript-graphql-request'
        config:
            useImplementingTypes: true
